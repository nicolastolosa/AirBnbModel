"""
Module that runs the neccesary scripts to load different files from the staging
database, preprocesses it and saves it into a new folder, so it can be fed
into a ML model.

This module is part of the AirBnbModel package. For further information on the
package, type:

>> help(AirBnbModel)
"""
